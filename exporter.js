function exportExcel(){const rows=[];rows.push(['表單名稱',document.getElementById('formTitle').value]);fields.forEach((f,i)=>{rows.push([`題目${i+1}`,f.title]);});const ws=XLSX.utils.aoa_to_sheet(rows);const wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,ws,'規格');XLSX.writeFile(wb,'表單規格.xlsx');}